export enum TrainSortField {
    ID        = 'id',
    NAME      = 'name',
    FROM      = 'from',
    TO        = 'to',
    DEPARTURE = 'departure',
    ARRIVAL   = 'arrival',
}

export enum SortOrder {
    ASC  = 'ASC',
    DESC = 'DESC'
}